import { submitHandler } from "../src/client/js/TripApp";
describe("The function should be defined", () => {
  test("Test submitHandler() function", () => {
    expect(submitHandler).toBeDefined();
  });
});
